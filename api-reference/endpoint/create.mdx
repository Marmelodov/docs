---
title: "Create Plant"
openapi: "POST /plants"
---

## I'm seeing what this looks like.

And here's how it looks.

```python
def some_code(arg):
    if this is in colors:
      syntax.highlighting(works):
        return True
    else:
      learn_more.MDX
        return False
```


